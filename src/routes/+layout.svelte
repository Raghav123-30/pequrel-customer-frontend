<script>
	import '../app.css';
	//import { locale, locales } from 'svelte-i18n';
</script>

<!-- <select bind:value={$locale}>
	{#each $locales as loc}
		<option value={loc}>{loc}</option>
	{/each}
</select> -->
<slot></slot>
